/*
module.exports.linkTo = function(text, address) {   
  return "<a href='" + text + "'>" + address + "</a>"
};
*/

module.exports.linkTo = function(text, address) {
  return "<a href='" + address + "'>" + text + "</a>"
};
